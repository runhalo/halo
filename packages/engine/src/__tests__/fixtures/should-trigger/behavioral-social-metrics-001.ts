// Fixture: behavioral-social-metrics-001 — SHOULD TRIGGER
function renderPost(post: any) {
  const likeCount = post.likes;
  const followerCount = post.followers;
  const leaderboard = computeRankings();
}
